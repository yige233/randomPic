{
  "port": 3000,
  "allowedPicExt": [".jpg", ".jpeg", ".png"],
  "dir": ["./pics"],
  "说明": "port是程序运行的端口，可不改；allowedPicExt是允许的图片扩展名；可不改；dir是要使用的图片所在的目录，应根据实际情况修改，Windows下路径中的反斜杠应改成正斜杠；也可以是数组，包含多个文件夹的路径"
}
